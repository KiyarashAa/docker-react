language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t kiyarashaa/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kiyarashaa/docker-react npm run test
